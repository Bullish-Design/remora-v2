from grail import Input, external

new_source: str = Input("new_source")


@external
async def propose_rewrite(new_source: str) -> str: ...


proposal_id = await propose_rewrite(new_source)
message = f"Rewrite proposal created: {proposal_id}"
message
