# Assumptions

- User wants strict interpretation of P0/P1 in recommendations doc plus sprint summary references.
- `graph_set_status` is included even though section label says P1 because user requested all P0/P1 and summary elevates it.
- Commit granularity is one recommendation item per commit.
- Push target is current branch upstream.
