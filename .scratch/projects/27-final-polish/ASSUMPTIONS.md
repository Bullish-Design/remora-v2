# Assumptions

- Scope is the full set of proposals A through G in `FINAL_POLISH_PROPOSAL.md`.
- Implement in proposal order (A, B, C, D, E, F, G).
- After each proposal: run targeted tests, commit, and push.
- Existing architecture conventions remain (event-driven flow, no plugin loader).
- Breaking changes are allowed where proposal explicitly replaces existing behavior (e.g., `apply_rewrite` replaced by proposal flow).
