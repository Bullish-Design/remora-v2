# Decisions

1. Use project42 tracker files as source-of-truth before code edits.
Reason: guide is multi-phase and intern left partial work; explicit tracking prevents regressions.
Assumptions: 1, 2, 5.

2. Treat `HEAD` and working-tree intern changes as partial implementation to be corrected, not reverted.
Reason: user requested review and continuation of intern work.
Assumptions: 2.
