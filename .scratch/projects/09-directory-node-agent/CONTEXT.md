# Context

## Current Status
Project scaffolding is initialized. No runtime code changes yet.

## Goal
Introduce directory nodes as first-class agents, including a root project/repo directory node that serves as graph entrypoint. Ensure file nodes have directory parents and directory nodes know parent/children relationships.

## Next Step
Finalize directory node ID and hierarchy invariants, then implement reconcile-time materialization with tests-first workflow.
