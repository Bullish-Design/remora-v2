# Decisions

## D1 (Initial): Root Entry Strategy
Use a root directory node as the canonical project/repo entrypoint for graph traversal and orchestration.

Rationale:
- Matches user requirement for a project-level entry node.
- Gives all file nodes a consistent ancestry path.
