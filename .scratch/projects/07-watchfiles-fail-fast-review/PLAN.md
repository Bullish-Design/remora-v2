# Plan

## NO SUBAGENTS - Do all work directly.

1. Read `.scratch/projects/05-dual-path-removals/BACKUP_PATH_REVIEW.md` and map watchfiles fallback code/test surface.
2. Update reconciler to remove polling backup paths and fail fast when watchfiles/path assumptions are invalid.
3. Update and run targeted tests for reconciler behavior; run broader validation as needed.
4. Commit and push watchfiles fail-fast changes.
5. Re-review `BACKUP_PATH_REVIEW.md` and author `REVIEW_REVIEW.md` in `.scratch/projects/05-dual-path-removals/` with deep analysis of other backup-path ideas.
6. Finalize project tracking docs.

## NO SUBAGENTS - Do all work directly.
