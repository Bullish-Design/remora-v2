# ASSUMPTIONS

1. The user wants a first-pass, implementation-derived Allium spec for the current Remora runtime, not a finalized product contract.
2. Distillation target is the runtime domain model and behavioral flow, not HTTP/LSP endpoint shape.
3. The spec should prefer stable domain behavior over incidental implementation mechanisms.
4. The current `src/remora` implementation is the source of truth for this pass.
