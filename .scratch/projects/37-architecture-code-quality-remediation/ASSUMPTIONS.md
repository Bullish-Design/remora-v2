# Assumptions

- Existing architecture permits incremental extraction without behavior changes.
- Remote `origin` is configured and push permissions are available.
- Focused verification is acceptable per item when full suite is expensive.
- Some recommendations may overlap (4.3 and 5.5); if so, each will still get explicit tracked implementation scope.
