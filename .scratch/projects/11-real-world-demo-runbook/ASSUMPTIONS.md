# Assumptions

- Demo is run from `/home/andrew/Documents/Projects/remora-v2` with `devenv` available.
- Presenter uses 3 terminals (`T1`, `T2`, `T3`) for runtime, controls, and SSE stream.
- Demo project is created in `/tmp/remora-realworld-demo`.
- Optional model-backed rewrite behavior depends on a reachable OpenAI-compatible model endpoint.
